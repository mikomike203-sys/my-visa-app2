export interface NavTab {
  id: string;
  label: string;
  icon: React.ElementType;
}

export interface ExpandedOption {
  id: string;
  label: string;
  icon: React.ElementType;
  color: string;
  bg: string;
}